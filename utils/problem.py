import numpy as np
from utils.utils import test, sparsity

def MNIST(x, **kwargs):
    spar = sparsity(kwargs['model'])
    acc = test(model, criterion, device, test_loader)
    if acc >= orig_acc - constrain:

    return